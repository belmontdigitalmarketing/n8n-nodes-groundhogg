// n8n community node entry point.
// n8n loads the credential and node classes directly from the paths defined in
// the "n8n" field of package.json — this file exists only so that `main` in
// package.json points somewhere valid.
module.exports = {};
